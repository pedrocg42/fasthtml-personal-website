# FastHTML Personal Website

This repository contains my personal website written in FastHTML. This is a project to learn how to create websites in FastHTML.

## Installation

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
uv venv --python 3.12
source .venv/bin/activate
uv pip install lume
lume -install
```
